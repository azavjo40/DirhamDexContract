{
  "name": "hardhat-contract",
  "version": "1.0.0",
  "scripts": {
    "cleanup": "npx rimraf cache artifacts typechain-types node_modules package-lock.json",
    "hardhat:compile": "npx hardhat compile",
    "hardhat:test": "npx hardhat test",
    "hardhat:deploy": "npx hardhat run scripts/deploy.ts --network sepolia",
    "hardhat:deploy:veryfy": "npx hardhat verify 'adress contract' --network sepolia"
  },
  "keywords": [],
  "author": "Azam Sufiev",
  "license": "ISC",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.2",
    "@openzeppelin/hardhat-upgrades": "^1.22.1",
    "dotenv": "^16.0.3",
    "hardhat": "^2.14.0",
    "hardhat-contract-sizer": "^2.8.0"
  },
  "dependencies": {
    "@chainlink/contracts": "^0.6.1",
    "@openzeppelin/contracts": "^4.8.2",
    "@openzeppelin/contracts-upgradeable": "^4.8.2",
    "hardhat-abi-exporter": "^2.10.1",
    "hardhat-gas-reporter": "^1.0.9"
  }
}
